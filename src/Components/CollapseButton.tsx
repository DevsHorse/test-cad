import React from "react";

class CollapseButton extends React.Component {
    render() {
        return (
            <button className="collapse-btn">Collapse lines</button>
        )
    }
}

export default CollapseButton;